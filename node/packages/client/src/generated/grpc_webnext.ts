// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.10
//   protoc               v7.35.1
// source: grpc_webnext.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "grpc.webnext.v1";

/**
 * A single WebSocket message. Exactly one field of `kind` is set.
 * Keepalive is NOT an application frame: it uses native WebSocket ping/pong
 * control frames (RFC 6455 §5.5.2), which browsers auto-answer and which generate
 * the wire traffic that keeps idle-timeout proxies from dropping a quiet stream.
 * Field numbers 6/7 held the removed app-level Ping/Pong and are reserved so they
 * are never reused.
 */
export interface Frame {
  /** open the stream (client -> server) */
  subscribe?:
    | Subscribe
    | undefined;
  /** one gRPC message, either direction */
  message?:
    | Message
    | undefined;
  /** client done sending (client -> server) */
  halfClose?:
    | HalfClose
    | undefined;
  /** stream end + status (server -> client) */
  trailer?:
    | Trailer
    | undefined;
  /** abort the stream, either direction */
  reset?:
    | Reset
    | undefined;
  /** initial response metadata (server -> client) */
  header?: Header | undefined;
}

/** One metadata entry. Binary values (keys ending in "-bin") use `bin_value`. */
export interface Metadatum {
  key: string;
  asciiValue?: string | undefined;
  binValue?: Uint8Array | undefined;
}

/** Open the stream. The first frame of the connection. */
export interface Subscribe {
  /** e.g. "/pkg.Service/Method" */
  method: string;
  /** request metadata */
  headers: Metadatum[];
  /** gRPC deadline; 0 = none */
  timeoutMillis: number;
  /** optional first message (unary-style) */
  initialPayload: Uint8Array;
  /** payloads are JSON (+json codec) not binary */
  json: boolean;
}

/** Initial response metadata, sent once before the first Message (if any). */
export interface Header {
  headers: Metadatum[];
}

export interface Message {
  /** a single encoded gRPC message */
  payload: Uint8Array;
}

/** Client is done sending (a bare marker). */
export interface HalfClose {
}

/** Stream completed. Carries gRPC status and trailing metadata. */
export interface Trailer {
  /** gRPC status code (0 = OK) */
  statusCode: number;
  statusMessage: string;
  trailers: Metadatum[];
}

/** Abort the stream. Also used by the server to reject it (e.g. feature disabled). */
export interface Reset {
  /** gRPC status code (e.g. CANCELLED, UNIMPLEMENTED) */
  statusCode: number;
  statusMessage: string;
}

function createBaseFrame(): Frame {
  return {
    subscribe: undefined,
    message: undefined,
    halfClose: undefined,
    trailer: undefined,
    reset: undefined,
    header: undefined,
  };
}

export const Frame: MessageFns<Frame> = {
  encode(message: Frame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subscribe !== undefined) {
      Subscribe.encode(message.subscribe, writer.uint32(10).fork()).join();
    }
    if (message.message !== undefined) {
      Message.encode(message.message, writer.uint32(18).fork()).join();
    }
    if (message.halfClose !== undefined) {
      HalfClose.encode(message.halfClose, writer.uint32(26).fork()).join();
    }
    if (message.trailer !== undefined) {
      Trailer.encode(message.trailer, writer.uint32(34).fork()).join();
    }
    if (message.reset !== undefined) {
      Reset.encode(message.reset, writer.uint32(42).fork()).join();
    }
    if (message.header !== undefined) {
      Header.encode(message.header, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Frame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscribe = Subscribe.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.halfClose = HalfClose.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.trailer = Trailer.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reset = Reset.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.header = Header.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Frame {
    return {
      subscribe: isSet(object.subscribe) ? Subscribe.fromJSON(object.subscribe) : undefined,
      message: isSet(object.message) ? Message.fromJSON(object.message) : undefined,
      halfClose: isSet(object.halfClose)
        ? HalfClose.fromJSON(object.halfClose)
        : isSet(object.half_close)
        ? HalfClose.fromJSON(object.half_close)
        : undefined,
      trailer: isSet(object.trailer) ? Trailer.fromJSON(object.trailer) : undefined,
      reset: isSet(object.reset) ? Reset.fromJSON(object.reset) : undefined,
      header: isSet(object.header) ? Header.fromJSON(object.header) : undefined,
    };
  },

  toJSON(message: Frame): unknown {
    const obj: any = {};
    if (message.subscribe !== undefined) {
      obj.subscribe = Subscribe.toJSON(message.subscribe);
    }
    if (message.message !== undefined) {
      obj.message = Message.toJSON(message.message);
    }
    if (message.halfClose !== undefined) {
      obj.halfClose = HalfClose.toJSON(message.halfClose);
    }
    if (message.trailer !== undefined) {
      obj.trailer = Trailer.toJSON(message.trailer);
    }
    if (message.reset !== undefined) {
      obj.reset = Reset.toJSON(message.reset);
    }
    if (message.header !== undefined) {
      obj.header = Header.toJSON(message.header);
    }
    return obj;
  },

  create(base?: DeepPartial<Frame>): Frame {
    return Frame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Frame>): Frame {
    const message = createBaseFrame();
    message.subscribe = (object.subscribe !== undefined && object.subscribe !== null)
      ? Subscribe.fromPartial(object.subscribe)
      : undefined;
    message.message = (object.message !== undefined && object.message !== null)
      ? Message.fromPartial(object.message)
      : undefined;
    message.halfClose = (object.halfClose !== undefined && object.halfClose !== null)
      ? HalfClose.fromPartial(object.halfClose)
      : undefined;
    message.trailer = (object.trailer !== undefined && object.trailer !== null)
      ? Trailer.fromPartial(object.trailer)
      : undefined;
    message.reset = (object.reset !== undefined && object.reset !== null) ? Reset.fromPartial(object.reset) : undefined;
    message.header = (object.header !== undefined && object.header !== null)
      ? Header.fromPartial(object.header)
      : undefined;
    return message;
  },
};

function createBaseMetadatum(): Metadatum {
  return { key: "", asciiValue: undefined, binValue: undefined };
}

export const Metadatum: MessageFns<Metadatum> = {
  encode(message: Metadatum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.asciiValue !== undefined) {
      writer.uint32(18).string(message.asciiValue);
    }
    if (message.binValue !== undefined) {
      writer.uint32(26).bytes(message.binValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Metadatum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadatum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.asciiValue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binValue = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metadatum {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      asciiValue: isSet(object.asciiValue)
        ? globalThis.String(object.asciiValue)
        : isSet(object.ascii_value)
        ? globalThis.String(object.ascii_value)
        : undefined,
      binValue: isSet(object.binValue)
        ? bytesFromBase64(object.binValue)
        : isSet(object.bin_value)
        ? bytesFromBase64(object.bin_value)
        : undefined,
    };
  },

  toJSON(message: Metadatum): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.asciiValue !== undefined) {
      obj.asciiValue = message.asciiValue;
    }
    if (message.binValue !== undefined) {
      obj.binValue = base64FromBytes(message.binValue);
    }
    return obj;
  },

  create(base?: DeepPartial<Metadatum>): Metadatum {
    return Metadatum.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Metadatum>): Metadatum {
    const message = createBaseMetadatum();
    message.key = object.key ?? "";
    message.asciiValue = object.asciiValue ?? undefined;
    message.binValue = object.binValue ?? undefined;
    return message;
  },
};

function createBaseSubscribe(): Subscribe {
  return { method: "", headers: [], timeoutMillis: 0, initialPayload: new Uint8Array(0), json: false };
}

export const Subscribe: MessageFns<Subscribe> = {
  encode(message: Subscribe, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    for (const v of message.headers) {
      Metadatum.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.timeoutMillis !== 0) {
      writer.uint32(24).uint32(message.timeoutMillis);
    }
    if (message.initialPayload.length !== 0) {
      writer.uint32(34).bytes(message.initialPayload);
    }
    if (message.json !== false) {
      writer.uint32(40).bool(message.json);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Subscribe {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribe();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.headers.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutMillis = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.initialPayload = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.json = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Subscribe {
    return {
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      headers: globalThis.Array.isArray(object?.headers) ? object.headers.map((e: any) => Metadatum.fromJSON(e)) : [],
      timeoutMillis: isSet(object.timeoutMillis)
        ? globalThis.Number(object.timeoutMillis)
        : isSet(object.timeout_millis)
        ? globalThis.Number(object.timeout_millis)
        : 0,
      initialPayload: isSet(object.initialPayload)
        ? bytesFromBase64(object.initialPayload)
        : isSet(object.initial_payload)
        ? bytesFromBase64(object.initial_payload)
        : new Uint8Array(0),
      json: isSet(object.json) ? globalThis.Boolean(object.json) : false,
    };
  },

  toJSON(message: Subscribe): unknown {
    const obj: any = {};
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => Metadatum.toJSON(e));
    }
    if (message.timeoutMillis !== 0) {
      obj.timeoutMillis = Math.round(message.timeoutMillis);
    }
    if (message.initialPayload.length !== 0) {
      obj.initialPayload = base64FromBytes(message.initialPayload);
    }
    if (message.json !== false) {
      obj.json = message.json;
    }
    return obj;
  },

  create(base?: DeepPartial<Subscribe>): Subscribe {
    return Subscribe.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Subscribe>): Subscribe {
    const message = createBaseSubscribe();
    message.method = object.method ?? "";
    message.headers = object.headers?.map((e) => Metadatum.fromPartial(e)) || [];
    message.timeoutMillis = object.timeoutMillis ?? 0;
    message.initialPayload = object.initialPayload ?? new Uint8Array(0);
    message.json = object.json ?? false;
    return message;
  },
};

function createBaseHeader(): Header {
  return { headers: [] };
}

export const Header: MessageFns<Header> = {
  encode(message: Header, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.headers) {
      Metadatum.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Header {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.headers.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Header {
    return {
      headers: globalThis.Array.isArray(object?.headers) ? object.headers.map((e: any) => Metadatum.fromJSON(e)) : [],
    };
  },

  toJSON(message: Header): unknown {
    const obj: any = {};
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => Metadatum.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Header>): Header {
    return Header.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Header>): Header {
    const message = createBaseHeader();
    message.headers = object.headers?.map((e) => Metadatum.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessage(): Message {
  return { payload: new Uint8Array(0) };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return { payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0) };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<Message>): Message {
    return Message.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Message>): Message {
    const message = createBaseMessage();
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseHalfClose(): HalfClose {
  return {};
}

export const HalfClose: MessageFns<HalfClose> = {
  encode(_: HalfClose, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HalfClose {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHalfClose();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HalfClose {
    return {};
  },

  toJSON(_: HalfClose): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<HalfClose>): HalfClose {
    return HalfClose.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<HalfClose>): HalfClose {
    const message = createBaseHalfClose();
    return message;
  },
};

function createBaseTrailer(): Trailer {
  return { statusCode: 0, statusMessage: "", trailers: [] };
}

export const Trailer: MessageFns<Trailer> = {
  encode(message: Trailer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statusCode !== 0) {
      writer.uint32(8).uint32(message.statusCode);
    }
    if (message.statusMessage !== "") {
      writer.uint32(18).string(message.statusMessage);
    }
    for (const v of message.trailers) {
      Metadatum.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Trailer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrailer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.trailers.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Trailer {
    return {
      statusCode: isSet(object.statusCode)
        ? globalThis.Number(object.statusCode)
        : isSet(object.status_code)
        ? globalThis.Number(object.status_code)
        : 0,
      statusMessage: isSet(object.statusMessage)
        ? globalThis.String(object.statusMessage)
        : isSet(object.status_message)
        ? globalThis.String(object.status_message)
        : "",
      trailers: globalThis.Array.isArray(object?.trailers)
        ? object.trailers.map((e: any) => Metadatum.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Trailer): unknown {
    const obj: any = {};
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.trailers?.length) {
      obj.trailers = message.trailers.map((e) => Metadatum.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Trailer>): Trailer {
    return Trailer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Trailer>): Trailer {
    const message = createBaseTrailer();
    message.statusCode = object.statusCode ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.trailers = object.trailers?.map((e) => Metadatum.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReset(): Reset {
  return { statusCode: 0, statusMessage: "" };
}

export const Reset: MessageFns<Reset> = {
  encode(message: Reset, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statusCode !== 0) {
      writer.uint32(8).uint32(message.statusCode);
    }
    if (message.statusMessage !== "") {
      writer.uint32(18).string(message.statusMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reset {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reset {
    return {
      statusCode: isSet(object.statusCode)
        ? globalThis.Number(object.statusCode)
        : isSet(object.status_code)
        ? globalThis.Number(object.status_code)
        : 0,
      statusMessage: isSet(object.statusMessage)
        ? globalThis.String(object.statusMessage)
        : isSet(object.status_message)
        ? globalThis.String(object.status_message)
        : "",
    };
  },

  toJSON(message: Reset): unknown {
    const obj: any = {};
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    return obj;
  },

  create(base?: DeepPartial<Reset>): Reset {
    return Reset.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reset>): Reset {
    const message = createBaseReset();
    message.statusCode = object.statusCode ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
