// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.10
//   protoc               v7.35.1
// source: conformance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "grpc.webnext.conformance.v1";

/**
 * How the server should respond. Present on unary requests and the first request of
 * a streaming RPC; ignored on subsequent streaming requests.
 */
export interface ResponseDefinition {
  /** Terminal gRPC status. code 0 = OK. For streaming, applied after all messages. */
  statusCode: number;
  statusMessage: string;
  /**
   * Metadata the server must emit. `headers` go out as initial metadata (WS Header
   * frame / Fetch response headers); `trailers` in the terminal Trailer/trailer block.
   */
  headers: Metadatum[];
  trailers: Metadatum[];
  /** Unary/bidi: the payload to echo back. Streaming: see `stream_messages`. */
  payload: Uint8Array;
  /** Server-streaming: the ordered messages to emit before the terminal status. */
  streamMessages: StreamMessage[];
  /**
   * Sleep this long before producing the (first) response. Drives deadline tests:
   * set delay > the case's grpc-timeout and expect DEADLINE_EXCEEDED (code 4).
   */
  delayMs: number;
  /**
   * If > 0, the unary/echo payload the server returns is this many bytes (server
   * fabricates it). Drives max-message-size cases (expect RESOURCE_EXHAUSTED = 8
   * when it exceeds the configured limit).
   */
  oversizeResponseBytes: number;
}

export interface StreamMessage {
  payload: Uint8Array;
  /** sleep before emitting this message */
  delayMs: number;
}

export interface UnaryRequest {
  payload: Uint8Array;
  responseDefinition: ResponseDefinition | undefined;
}

export interface ServerStreamRequest {
  responseDefinition: ResponseDefinition | undefined;
}

export interface ClientStreamRequest {
  payload: Uint8Array;
  /** honored on first request only */
  responseDefinition: ResponseDefinition | undefined;
}

export interface ClientStreamResponse {
  payload:
    | ConformancePayload
    | undefined;
  /** how many client messages the server saw */
  receivedCount: number;
  /** total payload bytes received */
  receivedBytes: Long;
}

export interface BidiStreamRequest {
  payload: Uint8Array;
  /** honored on first request only */
  responseDefinition: ResponseDefinition | undefined;
}

/**
 * What the server sends back. `request_info` lets a case assert what the server
 * actually received (echoed metadata, timeout, codec), catching silent drops.
 */
export interface ConformancePayload {
  payload: Uint8Array;
  requestInfo: RequestInfo | undefined;
}

export interface ClientStreamResponseInfo {
  receivedCount: number;
}

/** Observed request context, echoed for assertions. */
export interface RequestInfo {
  requestHeaders: Metadatum[];
  /** the grpc-timeout the server observed; 0 = none */
  timeoutMillis: number;
  /** did the request arrive on the +json codec */
  json: boolean;
}

/** One metadata entry. Binary values (keys ending in "-bin") use `bin_value`. */
export interface Metadatum {
  key: string;
  asciiValue?: string | undefined;
  binValue?: Uint8Array | undefined;
}

function createBaseResponseDefinition(): ResponseDefinition {
  return {
    statusCode: 0,
    statusMessage: "",
    headers: [],
    trailers: [],
    payload: new Uint8Array(0),
    streamMessages: [],
    delayMs: 0,
    oversizeResponseBytes: 0,
  };
}

export const ResponseDefinition: MessageFns<ResponseDefinition> = {
  encode(message: ResponseDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statusCode !== 0) {
      writer.uint32(8).uint32(message.statusCode);
    }
    if (message.statusMessage !== "") {
      writer.uint32(18).string(message.statusMessage);
    }
    for (const v of message.headers) {
      Metadatum.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.trailers) {
      Metadatum.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.payload.length !== 0) {
      writer.uint32(42).bytes(message.payload);
    }
    for (const v of message.streamMessages) {
      StreamMessage.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.delayMs !== 0) {
      writer.uint32(56).uint32(message.delayMs);
    }
    if (message.oversizeResponseBytes !== 0) {
      writer.uint32(64).uint32(message.oversizeResponseBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.headers.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.trailers.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.streamMessages.push(StreamMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.delayMs = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.oversizeResponseBytes = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseDefinition {
    return {
      statusCode: isSet(object.statusCode)
        ? globalThis.Number(object.statusCode)
        : isSet(object.status_code)
        ? globalThis.Number(object.status_code)
        : 0,
      statusMessage: isSet(object.statusMessage)
        ? globalThis.String(object.statusMessage)
        : isSet(object.status_message)
        ? globalThis.String(object.status_message)
        : "",
      headers: globalThis.Array.isArray(object?.headers) ? object.headers.map((e: any) => Metadatum.fromJSON(e)) : [],
      trailers: globalThis.Array.isArray(object?.trailers)
        ? object.trailers.map((e: any) => Metadatum.fromJSON(e))
        : [],
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      streamMessages: globalThis.Array.isArray(object?.streamMessages)
        ? object.streamMessages.map((e: any) => StreamMessage.fromJSON(e))
        : globalThis.Array.isArray(object?.stream_messages)
        ? object.stream_messages.map((e: any) => StreamMessage.fromJSON(e))
        : [],
      delayMs: isSet(object.delayMs)
        ? globalThis.Number(object.delayMs)
        : isSet(object.delay_ms)
        ? globalThis.Number(object.delay_ms)
        : 0,
      oversizeResponseBytes: isSet(object.oversizeResponseBytes)
        ? globalThis.Number(object.oversizeResponseBytes)
        : isSet(object.oversize_response_bytes)
        ? globalThis.Number(object.oversize_response_bytes)
        : 0,
    };
  },

  toJSON(message: ResponseDefinition): unknown {
    const obj: any = {};
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => Metadatum.toJSON(e));
    }
    if (message.trailers?.length) {
      obj.trailers = message.trailers.map((e) => Metadatum.toJSON(e));
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.streamMessages?.length) {
      obj.streamMessages = message.streamMessages.map((e) => StreamMessage.toJSON(e));
    }
    if (message.delayMs !== 0) {
      obj.delayMs = Math.round(message.delayMs);
    }
    if (message.oversizeResponseBytes !== 0) {
      obj.oversizeResponseBytes = Math.round(message.oversizeResponseBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<ResponseDefinition>): ResponseDefinition {
    return ResponseDefinition.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResponseDefinition>): ResponseDefinition {
    const message = createBaseResponseDefinition();
    message.statusCode = object.statusCode ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.headers = object.headers?.map((e) => Metadatum.fromPartial(e)) || [];
    message.trailers = object.trailers?.map((e) => Metadatum.fromPartial(e)) || [];
    message.payload = object.payload ?? new Uint8Array(0);
    message.streamMessages = object.streamMessages?.map((e) => StreamMessage.fromPartial(e)) || [];
    message.delayMs = object.delayMs ?? 0;
    message.oversizeResponseBytes = object.oversizeResponseBytes ?? 0;
    return message;
  },
};

function createBaseStreamMessage(): StreamMessage {
  return { payload: new Uint8Array(0), delayMs: 0 };
}

export const StreamMessage: MessageFns<StreamMessage> = {
  encode(message: StreamMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.delayMs !== 0) {
      writer.uint32(16).uint32(message.delayMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.delayMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamMessage {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      delayMs: isSet(object.delayMs)
        ? globalThis.Number(object.delayMs)
        : isSet(object.delay_ms)
        ? globalThis.Number(object.delay_ms)
        : 0,
    };
  },

  toJSON(message: StreamMessage): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.delayMs !== 0) {
      obj.delayMs = Math.round(message.delayMs);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamMessage>): StreamMessage {
    return StreamMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamMessage>): StreamMessage {
    const message = createBaseStreamMessage();
    message.payload = object.payload ?? new Uint8Array(0);
    message.delayMs = object.delayMs ?? 0;
    return message;
  },
};

function createBaseUnaryRequest(): UnaryRequest {
  return { payload: new Uint8Array(0), responseDefinition: undefined };
}

export const UnaryRequest: MessageFns<UnaryRequest> = {
  encode(message: UnaryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      ResponseDefinition.encode(message.responseDefinition, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnaryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnaryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.responseDefinition = ResponseDefinition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnaryRequest {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      responseDefinition: isSet(object.responseDefinition)
        ? ResponseDefinition.fromJSON(object.responseDefinition)
        : isSet(object.response_definition)
        ? ResponseDefinition.fromJSON(object.response_definition)
        : undefined,
    };
  },

  toJSON(message: UnaryRequest): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      obj.responseDefinition = ResponseDefinition.toJSON(message.responseDefinition);
    }
    return obj;
  },

  create(base?: DeepPartial<UnaryRequest>): UnaryRequest {
    return UnaryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnaryRequest>): UnaryRequest {
    const message = createBaseUnaryRequest();
    message.payload = object.payload ?? new Uint8Array(0);
    message.responseDefinition = (object.responseDefinition !== undefined && object.responseDefinition !== null)
      ? ResponseDefinition.fromPartial(object.responseDefinition)
      : undefined;
    return message;
  },
};

function createBaseServerStreamRequest(): ServerStreamRequest {
  return { responseDefinition: undefined };
}

export const ServerStreamRequest: MessageFns<ServerStreamRequest> = {
  encode(message: ServerStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseDefinition !== undefined) {
      ResponseDefinition.encode(message.responseDefinition, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseDefinition = ResponseDefinition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerStreamRequest {
    return {
      responseDefinition: isSet(object.responseDefinition)
        ? ResponseDefinition.fromJSON(object.responseDefinition)
        : isSet(object.response_definition)
        ? ResponseDefinition.fromJSON(object.response_definition)
        : undefined,
    };
  },

  toJSON(message: ServerStreamRequest): unknown {
    const obj: any = {};
    if (message.responseDefinition !== undefined) {
      obj.responseDefinition = ResponseDefinition.toJSON(message.responseDefinition);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerStreamRequest>): ServerStreamRequest {
    return ServerStreamRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerStreamRequest>): ServerStreamRequest {
    const message = createBaseServerStreamRequest();
    message.responseDefinition = (object.responseDefinition !== undefined && object.responseDefinition !== null)
      ? ResponseDefinition.fromPartial(object.responseDefinition)
      : undefined;
    return message;
  },
};

function createBaseClientStreamRequest(): ClientStreamRequest {
  return { payload: new Uint8Array(0), responseDefinition: undefined };
}

export const ClientStreamRequest: MessageFns<ClientStreamRequest> = {
  encode(message: ClientStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      ResponseDefinition.encode(message.responseDefinition, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.responseDefinition = ResponseDefinition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientStreamRequest {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      responseDefinition: isSet(object.responseDefinition)
        ? ResponseDefinition.fromJSON(object.responseDefinition)
        : isSet(object.response_definition)
        ? ResponseDefinition.fromJSON(object.response_definition)
        : undefined,
    };
  },

  toJSON(message: ClientStreamRequest): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      obj.responseDefinition = ResponseDefinition.toJSON(message.responseDefinition);
    }
    return obj;
  },

  create(base?: DeepPartial<ClientStreamRequest>): ClientStreamRequest {
    return ClientStreamRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClientStreamRequest>): ClientStreamRequest {
    const message = createBaseClientStreamRequest();
    message.payload = object.payload ?? new Uint8Array(0);
    message.responseDefinition = (object.responseDefinition !== undefined && object.responseDefinition !== null)
      ? ResponseDefinition.fromPartial(object.responseDefinition)
      : undefined;
    return message;
  },
};

function createBaseClientStreamResponse(): ClientStreamResponse {
  return { payload: undefined, receivedCount: 0, receivedBytes: Long.UZERO };
}

export const ClientStreamResponse: MessageFns<ClientStreamResponse> = {
  encode(message: ClientStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== undefined) {
      ConformancePayload.encode(message.payload, writer.uint32(10).fork()).join();
    }
    if (message.receivedCount !== 0) {
      writer.uint32(16).uint32(message.receivedCount);
    }
    if (!message.receivedBytes.equals(Long.UZERO)) {
      writer.uint32(24).uint64(message.receivedBytes.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = ConformancePayload.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.receivedCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.receivedBytes = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientStreamResponse {
    return {
      payload: isSet(object.payload) ? ConformancePayload.fromJSON(object.payload) : undefined,
      receivedCount: isSet(object.receivedCount)
        ? globalThis.Number(object.receivedCount)
        : isSet(object.received_count)
        ? globalThis.Number(object.received_count)
        : 0,
      receivedBytes: isSet(object.receivedBytes)
        ? Long.fromValue(object.receivedBytes)
        : isSet(object.received_bytes)
        ? Long.fromValue(object.received_bytes)
        : Long.UZERO,
    };
  },

  toJSON(message: ClientStreamResponse): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = ConformancePayload.toJSON(message.payload);
    }
    if (message.receivedCount !== 0) {
      obj.receivedCount = Math.round(message.receivedCount);
    }
    if (!message.receivedBytes.equals(Long.UZERO)) {
      obj.receivedBytes = (message.receivedBytes || Long.UZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<ClientStreamResponse>): ClientStreamResponse {
    return ClientStreamResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClientStreamResponse>): ClientStreamResponse {
    const message = createBaseClientStreamResponse();
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? ConformancePayload.fromPartial(object.payload)
      : undefined;
    message.receivedCount = object.receivedCount ?? 0;
    message.receivedBytes = (object.receivedBytes !== undefined && object.receivedBytes !== null)
      ? Long.fromValue(object.receivedBytes)
      : Long.UZERO;
    return message;
  },
};

function createBaseBidiStreamRequest(): BidiStreamRequest {
  return { payload: new Uint8Array(0), responseDefinition: undefined };
}

export const BidiStreamRequest: MessageFns<BidiStreamRequest> = {
  encode(message: BidiStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      ResponseDefinition.encode(message.responseDefinition, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BidiStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBidiStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.responseDefinition = ResponseDefinition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BidiStreamRequest {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      responseDefinition: isSet(object.responseDefinition)
        ? ResponseDefinition.fromJSON(object.responseDefinition)
        : isSet(object.response_definition)
        ? ResponseDefinition.fromJSON(object.response_definition)
        : undefined,
    };
  },

  toJSON(message: BidiStreamRequest): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.responseDefinition !== undefined) {
      obj.responseDefinition = ResponseDefinition.toJSON(message.responseDefinition);
    }
    return obj;
  },

  create(base?: DeepPartial<BidiStreamRequest>): BidiStreamRequest {
    return BidiStreamRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BidiStreamRequest>): BidiStreamRequest {
    const message = createBaseBidiStreamRequest();
    message.payload = object.payload ?? new Uint8Array(0);
    message.responseDefinition = (object.responseDefinition !== undefined && object.responseDefinition !== null)
      ? ResponseDefinition.fromPartial(object.responseDefinition)
      : undefined;
    return message;
  },
};

function createBaseConformancePayload(): ConformancePayload {
  return { payload: new Uint8Array(0), requestInfo: undefined };
}

export const ConformancePayload: MessageFns<ConformancePayload> = {
  encode(message: ConformancePayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.requestInfo !== undefined) {
      RequestInfo.encode(message.requestInfo, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConformancePayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConformancePayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestInfo = RequestInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConformancePayload {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      requestInfo: isSet(object.requestInfo)
        ? RequestInfo.fromJSON(object.requestInfo)
        : isSet(object.request_info)
        ? RequestInfo.fromJSON(object.request_info)
        : undefined,
    };
  },

  toJSON(message: ConformancePayload): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.requestInfo !== undefined) {
      obj.requestInfo = RequestInfo.toJSON(message.requestInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<ConformancePayload>): ConformancePayload {
    return ConformancePayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConformancePayload>): ConformancePayload {
    const message = createBaseConformancePayload();
    message.payload = object.payload ?? new Uint8Array(0);
    message.requestInfo = (object.requestInfo !== undefined && object.requestInfo !== null)
      ? RequestInfo.fromPartial(object.requestInfo)
      : undefined;
    return message;
  },
};

function createBaseClientStreamResponseInfo(): ClientStreamResponseInfo {
  return { receivedCount: 0 };
}

export const ClientStreamResponseInfo: MessageFns<ClientStreamResponseInfo> = {
  encode(message: ClientStreamResponseInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.receivedCount !== 0) {
      writer.uint32(8).uint32(message.receivedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientStreamResponseInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientStreamResponseInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.receivedCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientStreamResponseInfo {
    return {
      receivedCount: isSet(object.receivedCount)
        ? globalThis.Number(object.receivedCount)
        : isSet(object.received_count)
        ? globalThis.Number(object.received_count)
        : 0,
    };
  },

  toJSON(message: ClientStreamResponseInfo): unknown {
    const obj: any = {};
    if (message.receivedCount !== 0) {
      obj.receivedCount = Math.round(message.receivedCount);
    }
    return obj;
  },

  create(base?: DeepPartial<ClientStreamResponseInfo>): ClientStreamResponseInfo {
    return ClientStreamResponseInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClientStreamResponseInfo>): ClientStreamResponseInfo {
    const message = createBaseClientStreamResponseInfo();
    message.receivedCount = object.receivedCount ?? 0;
    return message;
  },
};

function createBaseRequestInfo(): RequestInfo {
  return { requestHeaders: [], timeoutMillis: 0, json: false };
}

export const RequestInfo: MessageFns<RequestInfo> = {
  encode(message: RequestInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requestHeaders) {
      Metadatum.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.timeoutMillis !== 0) {
      writer.uint32(16).uint32(message.timeoutMillis);
    }
    if (message.json !== false) {
      writer.uint32(24).bool(message.json);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestHeaders.push(Metadatum.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutMillis = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.json = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestInfo {
    return {
      requestHeaders: globalThis.Array.isArray(object?.requestHeaders)
        ? object.requestHeaders.map((e: any) => Metadatum.fromJSON(e))
        : globalThis.Array.isArray(object?.request_headers)
        ? object.request_headers.map((e: any) => Metadatum.fromJSON(e))
        : [],
      timeoutMillis: isSet(object.timeoutMillis)
        ? globalThis.Number(object.timeoutMillis)
        : isSet(object.timeout_millis)
        ? globalThis.Number(object.timeout_millis)
        : 0,
      json: isSet(object.json) ? globalThis.Boolean(object.json) : false,
    };
  },

  toJSON(message: RequestInfo): unknown {
    const obj: any = {};
    if (message.requestHeaders?.length) {
      obj.requestHeaders = message.requestHeaders.map((e) => Metadatum.toJSON(e));
    }
    if (message.timeoutMillis !== 0) {
      obj.timeoutMillis = Math.round(message.timeoutMillis);
    }
    if (message.json !== false) {
      obj.json = message.json;
    }
    return obj;
  },

  create(base?: DeepPartial<RequestInfo>): RequestInfo {
    return RequestInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestInfo>): RequestInfo {
    const message = createBaseRequestInfo();
    message.requestHeaders = object.requestHeaders?.map((e) => Metadatum.fromPartial(e)) || [];
    message.timeoutMillis = object.timeoutMillis ?? 0;
    message.json = object.json ?? false;
    return message;
  },
};

function createBaseMetadatum(): Metadatum {
  return { key: "", asciiValue: undefined, binValue: undefined };
}

export const Metadatum: MessageFns<Metadatum> = {
  encode(message: Metadatum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.asciiValue !== undefined) {
      writer.uint32(18).string(message.asciiValue);
    }
    if (message.binValue !== undefined) {
      writer.uint32(26).bytes(message.binValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Metadatum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadatum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.asciiValue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binValue = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metadatum {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      asciiValue: isSet(object.asciiValue)
        ? globalThis.String(object.asciiValue)
        : isSet(object.ascii_value)
        ? globalThis.String(object.ascii_value)
        : undefined,
      binValue: isSet(object.binValue)
        ? bytesFromBase64(object.binValue)
        : isSet(object.bin_value)
        ? bytesFromBase64(object.bin_value)
        : undefined,
    };
  },

  toJSON(message: Metadatum): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.asciiValue !== undefined) {
      obj.asciiValue = message.asciiValue;
    }
    if (message.binValue !== undefined) {
      obj.binValue = base64FromBytes(message.binValue);
    }
    return obj;
  },

  create(base?: DeepPartial<Metadatum>): Metadatum {
    return Metadatum.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Metadatum>): Metadatum {
    const message = createBaseMetadatum();
    message.key = object.key ?? "";
    message.asciiValue = object.asciiValue ?? undefined;
    message.binValue = object.binValue ?? undefined;
    return message;
  },
};

export type ConformanceServiceDefinition = typeof ConformanceServiceDefinition;
export const ConformanceServiceDefinition = {
  name: "ConformanceService",
  fullName: "grpc.webnext.conformance.v1.ConformanceService",
  methods: {
    /**
     * Unary over Fetch (or a single WebSocket stream). Echoes `payload` unless the
     * ResponseDefinition says otherwise; used for status, metadata, deadline, and
     * size-limit cases.
     */
    unary: {
      name: "Unary",
      requestType: UnaryRequest as typeof UnaryRequest,
      requestStream: false,
      responseType: ConformancePayload as typeof ConformancePayload,
      responseStream: false,
      options: {},
    },
    /**
     * Server streaming: emits ResponseDefinition.stream_messages in order, honoring
     * per-message `delay_ms`, then closes with ResponseDefinition.status.
     */
    serverStream: {
      name: "ServerStream",
      requestType: ServerStreamRequest as typeof ServerStreamRequest,
      requestStream: false,
      responseType: ConformancePayload as typeof ConformancePayload,
      responseStream: true,
      options: {},
    },
    /**
     * Client streaming: consumes the inbound stream, then returns one response built
     * from the ResponseDefinition on the FIRST request (later requests carry payload
     * only). `aggregate` reports what the server received.
     */
    clientStream: {
      name: "ClientStream",
      requestType: ClientStreamRequest as typeof ClientStreamRequest,
      requestStream: true,
      responseType: ClientStreamResponse as typeof ClientStreamResponse,
      responseStream: false,
      options: {},
    },
    /**
     * Bidi: for each inbound message, echoes one ConformancePayload back; closes with
     * the ResponseDefinition.status from the first request. Exercises interleaving,
     * half-close, and mid-stream Reset/cancel.
     */
    bidiStream: {
      name: "BidiStream",
      requestType: BidiStreamRequest as typeof BidiStreamRequest,
      requestStream: true,
      responseType: ConformancePayload as typeof ConformancePayload,
      responseStream: true,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
